datacenter = "rocha"
data_dir = "/opt/consul"

client_addr = "0.0.0.0"
ui_config{
  enabled = false
}

server = false
bind_addr = "0.0.0.0" # Listen on all IPv4
advertise_addr = "{{NOMAD_WORKERS_IP}}"
start_join = ["192.168.15.55"]
